import React from "react";

const Header: React.FC = () => {
  return (
    <header backgroundColor="lightblue" className="text-center py-3">
      <h1 className="display-4">Weather App</h1>
    </header>
  );
};

export default Header;
